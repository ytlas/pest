<?php
// Check if the privilege key is sent and correct
/* if((isset($post['key']) && $post['key'] === parse_ini_file($this->docroot.'/config.ini')['password'])){
 *     $sess['setup'] = true;
 * };*/
